{
  "name": "getql",
  "version": "0.0.8",
  "engines": {
    "node": ">=6"
  },
  "scripts": {
    "test": "ava --color -v -T 10000",
    "postinstall": "./node_modules/.bin/babel -d dist -s inline --no-comments src",
    "coverage": "nyc --check-coverage --lines 95 --functions 95 --branches 95 --per-file --reporter=lcov --reporter=text --reporter-dir ./coverage --clean --cache --instrument ava"
  },
  "description": "A GraphQL query framework for serverside apps",
  "main": "index.js",
  "repository": "https://github.com/khaosdoctor/qlector.git",
  "bugs": {
    "url": "https://github.com/khaosdoctor/qlector/issues"
  },
  "files": [
    "src"
  ],
  "author": "Lucas Santos <lhs.santoss@gmail.com>",
  "license": "MIT",
  "keywords": [
    "graphql",
    "query",
    "framework",
    "serverside",
    "graph",
    "post"
  ],
  "private": false,
  "dependencies": {
    "got": "^8.0.1"
  },
  "devDependencies": {
    "ava": "^0.24.0",
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "nyc": "^11.3.0",
    "sinon": "^4.1.2",
    "standard": "^10.0.3"
  }
}
