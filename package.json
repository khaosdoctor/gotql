{
  "name": "gotql",
  "version": "1.7.2",
  "engines": {
    "node": ">=8"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "npm run build:clean && ava --color -v -T 10000",
    "prepare": "npm run build:clean",
    "lint": "standard -v ./src",
    "build": "tsc",
    "clean": "rm -rf dist",
    "build:clean": "npm run clean && npm run build",
    "lint:fix": "standard --fix",
    "report": "cat ./coverage/lcov.info | codecov",
    "coverage": "npm run build:clean && nyc --check-coverage --per-file --reporter=lcov --reporter=text --reporter-dir ./coverage --clean --cache --instrument ava"
  },
  "description": "A GraphQL query framework for serverside apps",
  "main": "dist/index.js",
  "repository": "https://github.com/khaosdoctor/gotql.git",
  "bugs": {
    "url": "https://github.com/khaosdoctor/gotql/issues"
  },
  "pre-commit": [
    "build"
  ],
  "pre-push": [
    "lint",
    "coverage"
  ],
  "author": "Lucas Santos <lhs.santoss@gmail.com>",
  "license": "MIT",
  "keywords": [
    "graphql",
    "query",
    "framework",
    "serverside",
    "graph",
    "post"
  ],
  "private": false,
  "dependencies": {
    "debug": "^4.1.1",
    "got": "^9.6.0",
    "prepend-http": "^3.0.1"
  },
  "devDependencies": {
    "@types/debug": "^4.1.5",
    "@types/got": "^9.6.7",
    "@types/node": "^12.7.4",
    "ava": "^2.3.0",
    "codecov": "^3.5.0",
    "concurrently": "^4.1.2",
    "conduct": "^2.3.0",
    "nyc": "^14.1.1",
    "pre-push": "^0.1.1",
    "sinon": "^7.4.2",
    "standard": "^14.1.0",
    "tslint-config-standard": "^8.0.1",
    "typescript": "^3.6.2"
  }
}
